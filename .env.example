# Server San Clemente - Example Environment Variables
# Copy this file to ".env" and customize values for your environment.
# Never commit your real ".env" file to version control.

# -----------------------------------------------------------------------------
# Core server
# -----------------------------------------------------------------------------
# "development" | "production" | "test"
NODE_ENV=development

# HTTP port to bind the API server
PORT=3000

# Request body limits
JSON_LIMIT=1mb
URLENCODED_LIMIT=1mb

# Comma-separated list of allowed origins for CORS.
# Example: http://localhost:5173,http://localhost:3000,https://yourdomain.com
CORS_ORIGINS=http://localhost:3000

# -----------------------------------------------------------------------------
# Data storage
# -----------------------------------------------------------------------------
# Directory where JSON data files are stored. Relative paths are resolved
# from the project root. Defaults to "./data" if not set.
DATA_DIR=./data

# -----------------------------------------------------------------------------
# Admin seeding (optional)
# -----------------------------------------------------------------------------
# If the users collection is empty at startup and ADMIN_EMAIL + ADMIN_PASSWORD
# are provided, an admin user will be created automatically.
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=change_this_password
ADMIN_NAME=Admin

# -----------------------------------------------------------------------------
# Authentication & security
# -----------------------------------------------------------------------------
# Bcrypt work factor for password hashing (8-15 recommended; higher = slower).
BCRYPT_ROUNDS=12

# JWT signing algorithm. Default: HS256
JWT_ALGORITHM=HS256

# Either set a single shared secret via JWT_SECRET, or provide
# separate secrets for access and refresh tokens:
# JWT_SECRET is used as a fallback if type-specific secrets are not set.
JWT_SECRET=dev_change_me_please
# JWT_ACCESS_SECRET=
# JWT_REFRESH_SECRET=

# Optional issuer/audience claims to add/verify in JWTs
JWT_ISSUER=server-san-clemente
JWT_AUDIENCE=server-san-clemente-clients

# Token lifetimes
# Examples: "15m" (15 minutes), "7d" (7 days), "1h" (1 hour)
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# -----------------------------------------------------------------------------
# Notes
# -----------------------------------------------------------------------------
# - For development, the above defaults are OK.
# - For production, always set strong, unique values for JWT_* secrets and
#   ADMIN_PASSWORD (or disable seeding).
# - Ensure DATA_DIR points to a writeable, persistent location.
